import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.footerContent}>
        {/* Add your new footer content here */}
      </div>
    </footer>
  );
};

export default Footer;